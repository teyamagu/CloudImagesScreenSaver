# Copy to .env and fill in locally. Do not commit .env (see .gitignore).
# Used only for optional live API tests / CLI; the screen saver reads settings from ScreenSaverDefaults.
# DROPBOX_APP_KEY=
# DROPBOX_REFRESH_TOKEN=
# DROPBOX_TEST_FOLDER=/Photos
# DROPBOX_TEST_SKIP_DOWNLOAD=0
